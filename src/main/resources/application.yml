spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/wereis-main
    username: admin
    password: QazWsxEdc
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
